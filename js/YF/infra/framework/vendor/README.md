# Shared local vendor libraries

This directory will contain browser-ready local dependencies used by the common framework. No application may load these libraries from a CDN after Gate G1.

Planned libraries:

- jsPDF;
- svg2pdf;
- OpenType;
- Paper.js.

Each dependency must have a version, source file hash, license reference and explicit consumers before it is activated.

