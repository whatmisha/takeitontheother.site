# Mobile behavior

Opt in through `mobile` in `defineTool`, keep the canonical breakpoint and let
`MobileBootstrap` publish the viewport class. Application CSS may rearrange
domain panels and secondary actions but must retain the shared controls,
accessible labels, primary export and cleanup behavior. Test on both sides of
the documented breakpoint.
