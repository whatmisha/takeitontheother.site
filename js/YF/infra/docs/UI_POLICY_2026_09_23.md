# UI cleanup — 2026-09-23

Scope: the 16 active tools inside `upgrade/`. Original tools and artwork content are unchanged. These UI edits are local, not committed or published.

## Changes

- Pulsar Coder: removed the Payload and Ray Parameters headings, retained the Message accessible name on the input.
- Button captions show a shortcut only for the primary `mod+e` export. Other shortcuts, JSON visibility on J/О, and the help menu remain functional.
- Pattern 02: removed Undo/Redo buttons, retained history via `mod+z` / `mod+shift+z`. ToolUiController now supports explicit buttonless `group: 'keyboard'` commands with the same typing/dialog/lifecycle guards.
- Shared UI stylesheet: dark buttons use weight 400, including secondary PDF export, JSON, help, presets and primary-button dark hover/disabled states. Light primary buttons remain 500.
- All 16 tool documents declare English. Translated the remaining interface labels and user-facing errors in Hyperspace, Asterisk, Pattern 02, Calendar, Sticky Fingers, Pizza Boxer and Keyboarder. Artwork text, keyboard legends and Google Sheets column matching are not UI translations and were preserved.
- UI audit now checks dark-button weight and excess shortcut captions. Static tests cover all 16 HTML interfaces and Pizza fragments; framework tests cover caption policy and keyboard-only actions.
- Shared CSS is `uiq-5`, GeneratorHost `v=6`, new-tool entry scripts `integration-8`. Pizza's generated runtime was rebuilt. Its early diagnostic probe is now also in the source document, so a rebuild cannot discard it.

## Verification

692 Node tests passed across the main suites: framework 103, Sparky 200, Pizza 172, Wordplayer 7, Keyboarder 6, Sticky Fingers 10, Pulsar/Dither/Wander 51, catalog/audit/batch/Rays 143. Existing standalone boundary, worker, model and encoding checks passed too.

Also passed: isolation, original migration source checks (32 files / 791667 bytes), action/UI contracts, Component Lab and keyboard acceptance. No source tool outside `upgrade/` was written.

Browser checks at the local server:

- All 16 tools: English document language, no excess shortcut button captions, no weight-500 buttons with an explicit opaque black/dark-gray background, including currently hidden DOM buttons.
- Pattern 02: slider 50 → 51; undo → 50; redo → 51; restored to 50. No Undo/Redo buttons; both actions listed in help.
- Sparky, Pizza, Sticky Fingers, Keyboarder: J reveals JSON buttons without shortcut suffixes; weight 400; help retains export/import shortcuts.
- Pulsar: visual check without redundant headings; input remains readable and accessible.
- Pizza in UI audit: both new policy checks report no deviations; no console errors in the audit visit.

This pass does not claim exhaustive visual coverage of every popup/hover or a fresh manual export of every format. Renderer/export regression tests passed. Chladni microphone was not enabled during this UI-only pass.
