# Shared UI fonts

CoFo Sans Regular and Medium are copied from the isolated Keyboarder source snapshot during UPG-013. Their SHA-256 values must match `SOURCE_MANIFEST.json` entries for the original Keyboarder font files.

