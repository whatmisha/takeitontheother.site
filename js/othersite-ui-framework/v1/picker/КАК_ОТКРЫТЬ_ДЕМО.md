# 🚨 Как открыть демо

## Проблема с CORS

Файл `demo.html` использует ES6 модули, которые **не работают** при открытии через `file://` протокол.

Вы увидите ошибку:
```
Origin null is not allowed by Access-Control-Allow-Origin
```

## ✅ Решение 1: Используйте demo-standalone.html

Откройте файл **`demo-standalone.html`** — он работает БЕЗ веб-сервера!

```bash
open demo-standalone.html
```

Или просто дважды кликните на `demo-standalone.html` в Finder.

**Этот файл содержит весь код встроенным и работает сразу!**

## ✅ Решение 2: Запустите локальный сервер

Если хотите использовать оригинальный `demo.html` с модулями:

### Вариант А: Python (встроен в macOS)

```bash
# Перейдите в папку picker
cd /path/to/picker

# Запустите сервер
python3 -m http.server 8000

# Откройте в браузере:
# http://localhost:8000/demo.html
```

### Вариант Б: VS Code Live Server

1. Откройте папку `picker` в VS Code
2. Установите расширение "Live Server"
3. Правый клик на `demo.html` → "Open with Live Server"

### Вариант В: Node.js http-server

```bash
# Установите (один раз)
npm install -g http-server

# Запустите
http-server picker

# Откройте http://localhost:8080/demo.html
```

## 📊 Сравнение файлов

| Файл | Открытие | Модули | Размер |
|------|----------|--------|--------|
| `demo.html` | Нужен сервер | ES6 ✅ | 5 KB |
| `demo-standalone.html` | Сразу работает | Встроенные | 15 KB |

## 🎯 Рекомендация

**Для быстрого просмотра:** Используйте `demo-standalone.html`

**Для разработки:** Используйте `demo.html` + локальный сервер

---

**P.S.** Оба файла показывают одинаковый функционал, просто `demo-standalone.html` не требует веб-сервера!

