name: Grid Generator CI

on:
  push:
    paths:
      - 'js/YF/lunnen/grid_generator/**'
      - '.github/workflows/update-presets.yml'
  pull_request:
    paths:
      - 'js/YF/lunnen/grid_generator/**'
      - '.github/workflows/update-presets.yml'

concurrency:
  group: grid-generator-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: js/YF/lunnen/grid_generator/tools/package-lock.json

      - name: Install tooling
        run: npm ci --prefix js/YF/lunnen/grid_generator/tools

      - name: Verify source, presets, public runtime and tests
        run: npm --prefix js/YF/lunnen/grid_generator/tools test

      - name: Build production runtime
        run: npm --prefix js/YF/lunnen/grid_generator/tools run build
